---
# app-specific attributes
attributes:
  bc_num_hours:
    label: "Number of hours"
    widget: "number_field"
    value: 4
    min: 1
    step: 1
  advanced_cleanup_login:
    label: "Cleanup previous VS Code Tunnel login data"
    help: |
      Remove the previous login credentials file. This can be useful in case of authentication problems.
    widget: check_box
    value: "0"
  advanced_font_size:
    label: Font Size
    help: |
      Default terminal (xterm.js) font size.

      - Dynamic font/zoom adjustments via Ctrl+(Plus|Minux|0) has UI bugs.
      - To change font size, cancel your running interactive shell session and launch a new job with the desired font size.
    widget: number_field
    max: 30
    min: 10
    step: 1
    value: 16
  advanced_tmux_config:
    label: "Tmux: Use Bundled Config File"
    help: |
      - Checked = load config included with this interactive app
      - Unchecked = No config file is specified and Tmux will look in the default locations (usually <code>$HOME/.tmux.conf</code>)
        - Ensure your config file is compatible with Tmux 3.1b!
    widget: check_box
    value: "1"
<%= ERB.new(File.read(File.expand_path("../common_files/extra_attributes.yml.erb", __dir__)), eoutvar: "@common_attrs").result(binding) %>

form:
  - cluster
  - global_num_cores
  - dynamic_num_gpu_slots
  - global_bc_num_shards
  - bc_num_hours
  - global_working_dir
  - global_prerun
  - bc_email_on_started
  - global_advanced
  - auto_queues
  - global_advanced_num_nodes
  - advanced_font_size
  - advanced_tmux_config
  - advanced_cleanup_login
