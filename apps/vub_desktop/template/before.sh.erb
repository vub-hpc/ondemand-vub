# Export the module function if it exists
[[ $(type -t module) == "function" ]] && export -f module

# Set User Defined Working Directory
<%- if context.global_working_dir.blank? -%>
OOD_WD="$VSC_DATA"
<%- else -%>
OOD_WD="<%= context.global_working_dir.to_s %>"
<%- end -%>
export OOD_WD
