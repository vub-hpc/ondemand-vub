# Job submission configuration file
#
---
batch_connect:
  template: "basic"

script:
  native:
    - "--cpus-per-task=<%= num_cores.to_i %>"
    - "--gpus-per-node=<%= bc_num_gpu_slots.blank? ? 0 : bc_num_gpu_slots.to_i %>"
